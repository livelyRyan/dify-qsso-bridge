# Dify-QSSO-Bridge 环境变量配置示例
# 复制此文件为 .env 并填入实际值，供 OpenResty 使用

# ============================================
# 必填配置
# ============================================

# Dify 管理服务 - Token 交换 API 地址
# 此 API 负责将 QSSO token 转换为 Dify access_token 和 refresh_token
# Docker 环境使用 host.docker.internal 访问宿主机服务（Mac/Windows）
# Linux 环境可能需要使用 172.17.0.1 或其他网桥 IP
DIFY_TOKEN_BY_QSSO_API_BASE_URL=http://host.docker.internal:8087/get_dify_token

# Dify 应用首页 URL
# 用户登录成功后将重定向到此地址（带 access_token 和 refresh_token 参数）
# 示例：http://your-dify-domain.com/apps
DIFY_HOME_PAGE_BASE_URL=http://your-dify-domain.com/apps

# QSSO 登录页地址（包含回调地址）
# ret 参数指定登录成功后的回调地址（需要 URL 编码）
# 回调地址格式示例：http://your-dify-domain.com/qsso_backcall
# 完整示例：https://your-sso-domain.com/login.php?ret=http%3A%2F%2Fyour-dify-domain.com%2Fqsso_backcall
QSSO_LOGIN_URL=https://your-sso-domain.com/login.php?ret=http%3A%2F%2Fyour-dify-domain.com%2Fqsso_backcall
